# Колпинское шоссе, 127 и 129 — расчёт делителей с учётом каскада

Файл-результат: `splitters_kolpinskoe_127_129_v6.xlsx` (исходная книга + правки + блок «Сводка и проверка каскада»).

## 1. Итог одной строкой

**Правильный ответ — k = 2 замены (2×1×4 → 1×1×8), итог по двум домам: 112 шт. 1×4 и 40 шт. 1×8.**
Это не просто «минимальное» решение — это **единственное** решение при заданных правилах.
Обе замены нужно делать в **доме 127, парадные 12 и 14**.

| | было (v5) | стало (v6) |
|---|---|---|
| дом 127: 1×4 / 1×8 | 69 / 22 | **65 / 24** |
| дом 129: 1×4 / 1×8 | 47 / 16 | 47 / 16 (без изменений) |
| ИТОГО 1×4 | 116 (mod 8 = 4 ✗) | **112** (mod 8 = 0 ✓, mod 16 = 0 ✓) |
| ИТОГО 1×8 | 38 (mod 4 = 2 ✗) | **40** (mod 4 = 0 ✓, mod 8 = 0 ✓) |
| ёмкость / спрос | 768 / 768 | **768 / 768**, пустых 0 |
| пенетрация по парадным | 55,00…60,00 % | 55,00…60,00 % (не изменилась) |

Конкретные правки в книге: дом 127, п.12 и п.14 — `1×4: 3,75 → 1,75`, `1×8: 2 → 3`.
Ёмкость парадной не меняется (2×4 = 1×8), поэтому пенетрация и «0 пустых» сохраняются автоматически.

## 2. Настоящий смысл каскадного ограничения: PON 1:32

Формулировка «n4 кратно 8, n8 кратно 4» — верная, но это следствие более простого факта.
Обе разрешённые схемы каскада дают **ровно 32 абонентских порта на один порт OLT**:

```
1×4 → 4 шт. 1×8 → 32 порта     (тратит 1 шт. 1×4 каскада + 4 шт. 1×8 абонентских)
1×8 → 8 шт. 1×4 → 32 порта     (тратит 1 шт. 1×8 каскада + 8 шт. 1×4 абонентских)
```

Отсюда: если `a` — число PON-портов, собранных по первой схеме, `b` — по второй, то
`n8_абон = 4a`, `n4_абон = 8b`, а **суммарная ёмкость = 32·(a + b)**, то есть

> **Σ ёмкости (по обоим домам) обязана делиться на 32.**

Это ограничение жёстче, чем «кратно 4», и именно оно определяет весь расчёт.

**Проверка нашего решения:** 768 / 32 = **24 PON-порта**, из них
`a = n8/4 = 10` портов по схеме 1×4→1×8 и `b = n4/8 = 14` портов по схеме 1×8→1×4; 10 + 14 = 24 ✓.
Делителей 1-го каскада: **10 шт. 1×4 + 14 шт. 1×8**.
Всего железа: 1×4 — 112 + 10 = **122 шт.**, 1×8 — 40 + 14 = **54 шт.**

## 3. Почему суммарный спрос 768 — единственный вариант

* Максимум (D = U во всех парадных): 771 порт. Минимум (D = L): 746.
* Кратных 32 в диапазоне [746, 771] ровно одно число — **768**.
* Значит снять надо **ровно 3** абонентских порта, не больше и не меньше.
* Разбивка по домам: у каждого дома своя ёмкость должна быть кратна 4 (делители целые внутри дома).
  Единственная пара при Σ = 768 — **(452; 316)**, то есть все 3 порта снимаются **в доме 127**, дом 129 не трогаем.
* Ни у одной парадной нет запаса `U − L ≥ 2`, поэтому снимать надо по 1 порту в трёх разных парадных.
  Цена снятия = 100/K п.п., минимум — при максимальном K. K = 51 в доме 127 у п.12, п.14, п.22.

**Вывод: выбор codex (п.12, п.14, п.22) оптимален и единственен.** Каждая теряет 1,96 п.п. (до 56,86 %),
следующая по «дешевизне» парадная — п.23 (K = 43) со стоимостью 2,33 п.п., то есть хуже.

## 4. Почему k = 2, а не k = −2 (обратные замены)

При Σd = 768 количество 1×4 однозначно выражается через количество 1×8:

```
n4 = (768 − 8·n8) / 4 = 192 − 2·n8
```

Поэтому условия «n4 кратно 8» и «n8 кратно 4» **эквивалентны** — это одно ограничение, а не два.
Свободный параметр ровно один: n8 ∈ {…, 36, 40, 44, …}.

Ограничение «не более 4 шт. 1×4 на парадную» даёт нижнюю границу
`n8_i ≥ ⌈(d_i − 16)/8⌉`; сумма по всем парадным = 22 (дом 127) + 16 (дом 129) = **38**.
Текущая v5 уже стоит на этом минимуме, значит:

* **n8 = 36 (k = −2) физически невозможен** — правило «≤ 4 шт. 1×4 в парадной» будет нарушено;
* **n8 = 40 (k = +2) — минимально возможное решение**;
* n8 = 44, 48, … допустимы, но это лишние 1×8 и меньше 1×4 — против пожелания «1×4 на этаже».

Дополнительно: в книге считаются ещё и «строгие» проверки (`n4/8` затем `/2`, т.е. n4 кратно 16;
`n8/4` затем `/2`, т.е. n8 кратно 8). Их проходят только n8 ∈ {40, 48, 56, …} — и **40 снова минимум**.
Вариант n8 = 44 «строгую» проверку не проходит. Так что 112 / 40 устойчив к обеим трактовкам.

## 5. Почему обе замены — именно в доме 127 (п.12 и п.14)

1. **K = 51 — самые крупные парадные.** 1×8 закрывает 8 портов ≈ 13–14 квартир; чем выше стояк,
   тем меньше «пустых этажей» относительно парадной. Плюс в п.12/п.14 есть коммерция (C = 5),
   её порт логично вешать на нижний делитель.
2. **Каскад по домам сходится ровно.** Если обе замены сделать в доме 127, получаем
   `n8: 24 и 16` — оба кратны 4, т.е. ветви «1×4 → 4×1×8» **не пересекают границу домов**.
   При любом другом размещении (обе в 129 или по одной в каждый дом) кратность 4 ломается в обоих домах,
   и придётся тянуть ещё одну общую ветвь между домами.
3. **Общая ветвь между домами всё равно нужна ровно одна.** По 1×4: 65 (дом 127) = 8 групп по 8 + 1 остаток,
   47 (дом 129) = 5 групп по 8 + 7 остаток; 1 + 7 = 8 → один каскадный 1×8 кормит 1 делитель в 127 и 7 в 129.
   Избежать этого нельзя в принципе: `n4_127 = 113 − 2·n8_127` всегда **нечётно**, значит на 8 не делится
   ни при каком раскладе. **Поэтому каскад обязан считаться по двум домам сразу — по отдельности задача неразрешима.**

Итоговая раскладка каскада: дом 127 — 8 ветвей «1×8 → 8×1×4» и 6 ветвей «1×4 → 4×1×8»;
дом 129 — 5 ветвей «1×8 → 8×1×4» и 4 ветви «1×4 → 4×1×8»; плюс 1 общая ветвь «1×8 → 8×1×4» (1 шт. в 127 + 7 шт. в 129).
Всего 14 + 10 = 24 PON-порта.

## 6. Ответы на заданные вопросы

| Вопрос | Ответ |
|---|---|
| Правильно ли понято каскадное ограничение? | Да, но точнее оно формулируется как «ёмкость кратна 32» (PON 1:32). Условия для 1×4 и 1×8 не независимы — это одно и то же условие. |
| Единственное ли это решение? | По количествам — **да**: n8 = 40 / n4 = 112 единственный минимум, вариант «−2» невозможен, варианты 44+ хуже по мягким критериям. По размещению — есть свобода, но она снимается критериями из п.5. |
| Какие 2 парадные выбрать? | **Дом 127, п.12 и п.14** (K = 51, есть коммерция, каскад сходится по домам без лишних междомовых ветвей). |
| Может, лучше поменять распределение D_i? | Нельзя: Σd = 768 — единственный допустимый суммарный спрос, разбивка (452; 316) единственная, D = U всюду кроме трёх K = 51. Текущее распределение уже максимально по пенетрации. |
| Нужен ли обратный каскад 1×8 → 1×4? | **Да, он уже используется** — это и есть ветвь `b = 14`. В сети одновременно работают обе схемы: 10 ветвей 1×4→1×8 и 14 ветвей 1×8→1×4. |

## 7. Мелкие замечания по книге

* `Z19` и `S34` считают **среднее арифметическое** пенетраций по парадным, а не по дому.
  Средневзвешенные значения: дом 127 — **58,44 %**, дом 129 — **58,84 %** (добавлены в `AA19` и `T34`).
* `S26` (сумма коммерции по дому 129) отсутствовала — добавлена.
* `Z12`/`Z13` («абон. в подвале») содержали вручную вписанные итоги 69/22 — обновлены до 65/24.
